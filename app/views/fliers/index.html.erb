<!DOCTYPE html>
<html>
  <head>
    <%= stylesheet_link_tag    "application", media: "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= render 'layouts/header' %>
  </head>
  <div id="body">

    <div class="container">

      <br/><br/>

        <h1> What's happening, right now. </h1>

          <% @fliers.each do |flier| %>

            <div class="fliers shadow">
                <div class="flierdata">
                      <%= image_tag flier.image.url(:thumb), class: "flimg" %>
                      <%= link_to flier.title, show_flier_path(:id => flier.id), class: "fltit" %>

                          <% if @fliers_for_adding.include?(flier) %>
                          <%= button_to 'Addddddd to MyBoard', myboard_add_flier_path(:flier_id => flier.id), class: "btn fladd" %>
                          <% else %>
                          <p> Already added </p>
                          <%end%>
                </div>
            </div>

          <%end%>

        </br></br></br></br>
    </div>
  </div>
  <div id="footer"><%= render 'layouts/footer' %></div>
</html>

